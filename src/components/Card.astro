---
interface Props {
  className: string;
}

const { className }: Props = Astro.props;
---

<article
  class={`rounded-md border flex flex-col items-center p-6 ${className}`}
>
  <slot />
</article>
